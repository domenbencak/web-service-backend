<!DOCTYPE html>
<html>
<head>
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/5.0.0/css/bootstrap.min.css">
    <style>
        /* Center the form */
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f5f5f5;
        }
        /* Add straight border */
        .rounded-form {
            border: 2px solid #cccccc;
            border-radius: 0;
            padding: 40px;
            background-color: #ffffff;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }
        /* Center the text */
        .center-text {
            text-align: center;
            text-transform: uppercase;
            color: #333333;
            margin-bottom: 40px;
        }

        label{
            font-weight: bold;
        }
        td{
            padding-top: 10px;
            padding-right: 10px;
        }
        button{
            width: 230px;
        }
        .submit-tr {
            margin-top: 20px;
        }
        .create-account-tr {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="rounded-form">
        <h1 class="center-text">Prijava</h1>
        <form action="login" method="post" class="mb-3">
            <table>
                <tr>
                    <td><label for="username">Uporabniško ime:</label></td>
                    <td><input type="text" class="form-control" id="username" name="username" required></td>
                </tr>
                <tr>
                    <td><label for="password">Geslo:</label></td>
                    <td><input type="password" class="form-control" id="password" name="password" required></td>
                </tr>
                <tr class="submit-tr">
                    <td></td>
                    <td>                         
                        <div class="d-grid gap-2">
                            <button class="btn btn-primary" type="submit">Prijava</button>
                        </div>
                    </td>
                </tr>
                <tr class="create-account-tr">
                    <td colspan="2"><p><a href="/user/register">Ustvari si račun</a></p></td>
                </tr>
            </table>
        </form>
    </div>
    
    <!-- Include Bootstrap JS (optional) -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/5.0.0/js/bootstrap.min.js"></script>
</body>
</html>