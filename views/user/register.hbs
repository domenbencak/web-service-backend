<!DOCTYPE html>
<html>
<head>
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/5.0.0/css/bootstrap.min.css">
    <style>
        /* Center the form */
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f5f5f5;
        }
        /* Add straight border */
        .rounded-form {
            border: 2px solid #cccccc;
            border-radius: 0;
            padding: 40px;
            background-color: #ffffff;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.1);
        }
        /* Center the text */
        .center-text {
            text-align: center;
            text-transform: uppercase;
            color: #333333;
            margin-bottom: 40px;
        }

        label {
            font-weight: bold;
        }
        td {
            padding-top: 10px;
            padding-right: 10px;
        }
        .btn {
            width: 300px;
        }
        .submit-tr {
            margin-top: 20px;
        }
        .create-account-tr {
            margin-top: 10px;
        }
        .login-label {
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="rounded-form">
        <h1 class="center-text">Registracija</h1>
        <form action="/user" method="post" class="mb-3">
            <table>
                <tr>
                    <td><label for="email">E-pošta:</label></td>
                    <td><input type="email" class="form-control" id="email" name="email" required></td>
                </tr>
                <tr>
                    <td><label for="username">Uporabniško ime:</label></td>
                    <td><input type="text" class="form-control" id="username" name="username" required></td>
                </tr>
                <tr>
                    <td><label for="password">Geslo:</label></td>
                    <td><input type="password" class="form-control" id="password" name="password" required></td>
                </tr>
                <tr class="submit-tr">
                    <td></td>
                    <td>                         
                        <div class="d-grid gap-2">
                            <input type="submit" class="btn btn-primary" value="Registriraj se">
                        </div>
                    </td>
                </tr>
                <tr>
                    <td><a href="/user/login">Prijavi se</a> </td>
                    <td> </td>
                </tr>
            </table>
        </form>    
        </div>
    
    <!-- Include Bootstrap JS (optional) -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/5.0.0/js/bootstrap.min.js"></script>
</body>
</html>